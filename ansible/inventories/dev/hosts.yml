---
# Dev inventory: the local single-node Docker Swarm (WSL2).
# Ansible drives the local Docker CLI directly (connection: local).
all:
  hosts:
    localhost:
      ansible_connection: local
      ansible_python_interpreter: /usr/bin/python3
