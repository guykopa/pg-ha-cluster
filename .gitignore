# Local environment files (never commit real secret values)
.env
*.env

# Generated dev credentials (docker secrets are the source of truth)
secrets/
!ansible/roles/secrets/

# Ansible
*.retry
ansible/**/vault.yml
ansible/**/*.vault
ansible/.vault_pass

# Vagrant (multi-node target, if provisioned locally later)
.vagrant/

# OS / editor noise
.DS_Store
*~
